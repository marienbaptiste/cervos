# Custom streaming diarization — pyannote components + faster-whisper
# No diart dependency. Uses pyannote segmentation + embedding models directly.
numpy==1.26.4
torch>=2.6.0,<2.7.0
torchaudio>=2.6.0,<2.7.0
pyannote.audio==3.1.1
faster-whisper>=1.1.0
# WhisperLive installed separately in Dockerfile.base (--no-deps to avoid numpy conflict)
# Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.2
python-multipart==0.0.20
chromadb>=0.6.0
requests
huggingface_hub<0.25
